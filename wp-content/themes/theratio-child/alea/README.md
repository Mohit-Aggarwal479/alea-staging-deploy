# ALEA redesign modules

Self-contained page templates + design-system CSS for the staging redesign.
Loaded by functions.php. Deployed by .cpanel.yml (staging only).

- design-system.css  — tokens + component kit for the whole site
- page-*.php         — one file per page TEMPLATE (a template serves many URLs)
